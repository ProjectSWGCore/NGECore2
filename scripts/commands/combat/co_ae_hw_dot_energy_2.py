import sys

def setup(core, actor, target, command):
	command.setDotDuration(15)
	command.setDotType('energy')
	command.setDotIntensity(125)
	return
	
def run(core, actor, target, commandString):
	return
