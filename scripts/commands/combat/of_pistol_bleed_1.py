import sys

def setup(core, actor, target, command):
	command.setBuffNameTarget('of_pistol_bleed')
	return
	
def preRun(core, actor, target, command):
	return

def run(core, actor, target, commandString):
	return